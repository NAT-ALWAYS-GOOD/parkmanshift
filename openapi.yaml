openapi: 3.0.3
info:
  title: Parkmanshift API
  version: 1.0.0
  description: Spécification de l'API pour le backend Parkmanshift — contrat d'interface partagé backend/web.
servers:
  - url: http://localhost:8080
    description: Serveur local par défaut
tags:
  - name: hello
    description: Walking skeleton endpoint
  - name: auth
    description: Authentification et gestion des utilisateurs
  - name: parking
    description: Gestion du parking
security:
  - BearerAuth: []
paths:
  /api/hello:
    get:
      security: []
      tags:
        - hello
      summary: Récupérer un message de salutation (walking skeleton)
      operationId: hello
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string
                example: "Walking Skeleton"
  /api/auth/login:
    post:
      security: []
      tags:
        - auth
      summary: Se connecter pour obtenir un token JWT
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                password:
                  type: string
      responses:
        '200':
          description: Connexion réussie
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
  /api/auth/register-employee:
    post:
      tags:
        - auth
      summary: Enregistrer un nouvel employé (réservé aux secrétaires)
      operationId: registerEmployee
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRegistrationDto'
      responses:
        '200':
          description: Utilisateur créé avec succès
  /api/parking/state:
    get:
      tags:
        - parking
      summary: Récupérer l'état des places de parking
      operationId: getParkingState
      parameters:
        - name: date
          in: query
          description: Date pour laquelle récupérer l'état (par défaut la date du jour si omis)
          required: false
          schema:
            type: string
            format: date
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SpotStateDto'
  /api/parking/reservations:
    post:
      tags:
        - parking
      summary: Réserver une place de parking
      operationId: reserveSpot
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReserveRequestDto'
      responses:
        '200':
          description: Réservation créée avec succès
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReservationDto'
  /api/parking/reservations/{id}/checkin:
    post:
      tags:
        - parking
      summary: Signaler l'arrivée (check-in) pour une réservation
      operationId: checkIn
      parameters:
        - name: id
          in: path
          required: true
          description: ID (UUID) de la réservation
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Check-in effectué avec succès
  /api/parking/reservations/{id}:
    delete:
      tags:
        - parking
      summary: Annuler une réservation
      operationId: cancelReservation
      parameters:
        - name: id
          in: path
          required: true
          description: ID (UUID) de la réservation à annuler
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Annulation effectuée avec succès

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    SpotStateDto:
      type: object
      properties:
        spot:
          $ref: '#/components/schemas/ParkingSpotDto'
        status:
          $ref: '#/components/schemas/SpotStatusDto'
    ParkingSpotDto:
      type: object
      properties:
        label:
          type: string
          example: "P-01"
        type:
          $ref: '#/components/schemas/SpotTypeDto'
    ReservationDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        parkingSpotLabel:
          type: string
          example: "P-01"
        employeeId:
          type: string
          example: "EMP-1234"
        date:
          type: string
          format: date
        status:
          $ref: '#/components/schemas/ReservationStatusDto'
    ReserveRequestDto:
      type: object
      required:
        - parkingSpotLabel
        - date
      properties:
        parkingSpotLabel:
          type: string
          example: "P-01"
        date:
          type: string
          format: date
    UserRegistrationDto:
      type: object
      required:
        - username
        - password
      properties:
        username:
          type: string
        password:
          type: string
        role:
          type: string
          enum: [EMPLOYEE, MANAGER, SECRETARY]
          default: EMPLOYEE
    SpotStatusDto:
      type: string
      enum:
        - FREE
        - RESERVED
        - OCCUPIED
        - UNAVAILABLE
    SpotTypeDto:
      type: string
      enum:
        - ELECTRIC
        - THERMAL
    ReservationStatusDto:
      type: string
      enum:
        - RESERVED
        - OCCUPIED
        - CANCELLED
